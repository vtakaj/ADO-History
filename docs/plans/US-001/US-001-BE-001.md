# US-001-BE-001: Implement Azure DevOps API connection and PAT auth

## Task Overview
Implement connectivity to the Azure DevOps REST API and authentication via Personal Access Token (PAT).

## Acceptance Criteria
- [x] Create a connection function to Azure DevOps REST API v7.2
- [x] Send HTTP requests using PAT authentication
- [x] API connection test feature (connectivity check)
- [x] Show appropriate error messages on auth errors
- [x] Parameterize organization and project names

## Technical Requirements
- Use curl for HTTP communication
- Set PAT auth header via Base64 encoding
- Determine responses via HTTP status codes
- Use API version 7.2

## Implementation Details
```bash
# Target functions
call_ado_api() {
    local endpoint="$1"
    # PAT auth with curl -s -u ":${PAT}"
    # Check HTTP status code
    # Proper handling on errors
}

test_api_connection() {
    # API connectivity test
    # Auth check
}
```

## Definition of Done
- [x] Azure DevOps API connects successfully via curl
- [x] Clear error messages are shown on auth errors
- [x] API connection test succeeds
- [x] Organization and project names can be set via environment variables

## Dependencies
- US-001-INF-001 (base structure)
- US-001-INF-003 (configuration management)

## Estimated Effort
2-3 hours

## Testing Strategy
- Connection test with a valid PAT
- Error handling test with an invalid PAT
- Behavior check on network errors